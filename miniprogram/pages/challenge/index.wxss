@import "../../styles/theme.wxss";

.page-challenge {
  min-height: 100vh;
  background: var(--color-bg);
  padding: 32rpx;
  padding-bottom: calc(32rpx + env(safe-area-inset-bottom));
}

/* 进行中的挑战 */
.active-challenges {
  margin-bottom: 40rpx;
}

.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 24rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--color-text);
}

.section-count {
  font-size: 24rpx;
  color: var(--color-text-secondary);
}

.section-subtitle {
  font-size: 24rpx;
  color: var(--color-text-secondary);
  margin-top: 8rpx;
  display: block;
}

.challenge-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.challenge-item {
  padding: 24rpx;
  transition: all 0.3s ease;
}

.challenge-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.challenge-left {
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex: 1;
}

.challenge-icon-wrap {
  width: 72rpx;
  height: 72rpx;
  border-radius: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.challenge-icon-text {
  font-size: 36rpx;
}

.challenge-info {
  flex: 1;
}

.challenge-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #2C3E2D;
  display: block;
  margin-bottom: 10rpx;
}

.challenge-progress-bar {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.progress-track {
  flex: 1;
  height: 12rpx;
  background: var(--color-border);
  border-radius: 6rpx;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  border-radius: 6rpx;
  transition: width 0.5s ease;
}

.progress-text {
  font-size: 22rpx;
  color: var(--color-primary);
  font-weight: 600;
  min-width: 60rpx;
  text-align: right;
}

.challenge-right {
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex-shrink: 0;
  margin-left: 16rpx;
}

.challenge-stat {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.stat-num {
  font-size: 28rpx;
  font-weight: 700;
  color: #5B9A6F;
}

.stat-label {
  font-size: 20rpx;
  color: #7A8F7C;
}

.expand-arrow {
  font-size: 32rpx;
  color: #ccc;
  transition: transform 0.3s ease;
  transform: rotate(90deg);
}

.expand-arrow.expanded {
  transform: rotate(270deg);
}

/* 展开详情 */
.challenge-detail {
  margin-top: 24rpx;
  padding-top: 24rpx;
  border-top: 2rpx solid #E8F5E9;
}

.timeline {
  padding: 0;
}

.timeline-item {
  display: flex;
  position: relative;
  padding-left: 40rpx;
  padding-bottom: 24rpx;
}

.timeline-item:last-child {
  padding-bottom: 0;
}

.timeline-dot {
  position: absolute;
  left: 0;
  top: 8rpx;
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: var(--color-border);
  z-index: 1;
}

.timeline-item.passed .timeline-dot {
  background: var(--color-primary);
}

.timeline-item.current .timeline-dot {
  background: var(--color-accent);
  box-shadow: 0 0 0 6rpx rgba(232, 184, 109, 0.2);
}

.timeline-line {
  position: absolute;
  left: 7rpx;
  top: 24rpx;
  width: 2rpx;
  height: calc(100% - 16rpx);
  background: var(--color-border);
}

.timeline-item.passed .timeline-line {
  background: rgba(91, 154, 111, 0.3);
}

.timeline-content {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.timeline-day {
  font-size: 24rpx;
  font-weight: 600;
  color: var(--color-text);
  min-width: 80rpx;
}

.timeline-item.passed .timeline-day {
  color: var(--color-primary);
}

.timeline-item.current .timeline-day {
  color: var(--color-accent);
}

.timeline-desc {
  font-size: 22rpx;
  color: var(--color-text-secondary);
}

/* 目标选择器 */
.goal-selector {
  margin-bottom: 24rpx;
}

.selector-label {
  font-size: 24rpx;
  color: var(--color-text-secondary);
  margin-bottom: 12rpx;
  display: block;
}

.goal-scroll {
  white-space: nowrap;
}

.goal-scroll::-webkit-scrollbar {
  display: none;
}

.goal-chip {
  display: inline-flex;
  align-items: center;
  gap: 6rpx;
  padding: 12rpx 24rpx;
  margin-right: 12rpx;
  border-radius: 24rpx;
  background: var(--color-bg-card);
  font-size: 24rpx;
  color: var(--color-text-secondary);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
  border: 2rpx solid transparent;
  transition: all 0.2s ease;
}

.goal-chip.selected {
  background: rgba(91, 154, 111, 0.1);
  border-color: var(--color-primary);
  color: var(--color-primary);
}

.chip-icon {
  font-size: 24rpx;
}

/* 挑战卡片网格 */
.challenge-grid {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20rpx;
  margin-bottom: 40rpx;
}

.challenge-card {
  width: calc(50% - 10rpx);
  background: var(--color-bg-card);
  border-radius: var(--radius-lg);
  padding: 32rpx 24rpx;
  box-shadow: var(--shadow-sm);
  display: flex;
  flex-direction: column;
  align-items: center;
  transition: all 0.3s ease;
  border: 2rpx solid transparent;
  text-align: center;
}

.challenge-card.selected {
  border-color: var(--color-primary);
  box-shadow: var(--shadow-md);
  transform: scale(1.02);
}

.challenge-card:active {
  transform: scale(0.96);
}

.card-icon {
  font-size: 48rpx;
  margin-bottom: 12rpx;
  line-height: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 80rpx;
  height: 80rpx;
}

.card-days {
  font-size: 56rpx;
  font-weight: 700;
  color: var(--color-primary);
  line-height: 1;
}

.card-unit {
  font-size: 24rpx;
  color: var(--color-text-secondary);
  margin-top: 4rpx;
}

.card-name {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--color-text);
  margin-top: 12rpx;
}

.card-desc {
  font-size: 22rpx;
  color: var(--color-text-secondary);
  margin-top: 4rpx;
}

.btn-start {
  width: 100%;
  height: 96rpx;
  background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
  color: #fff;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: var(--radius-xl);
  border: none;
  box-shadow: var(--shadow-md);
}

.btn-start[disabled] {
  opacity: 0.5;
}

.btn-start::after {
  display: none;
}

/* 已完成/失败的挑战 */
.completed-challenges,
.failed-challenges {
  margin-top: 40rpx;
}

.challenge-history-list {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.history-item {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 20rpx;
  background: var(--color-bg-card);
  border-radius: 12rpx;
}

.history-item.failed {
  opacity: 0.8;
}

.history-icon {
  font-size: 32rpx;
}

.history-info {
  flex: 1;
}

.history-name {
  font-size: 26rpx;
  font-weight: 600;
  color: var(--color-text);
  display: block;
}

.history-date {
  font-size: 22rpx;
  color: var(--color-text-secondary);
  display: block;
  margin-top: 4rpx;
}

.history-action {
  padding: 8rpx 20rpx;
  background: rgba(91, 154, 111, 0.1);
  border-radius: 16rpx;
}

.retry-text {
  font-size: 24rpx;
  color: var(--color-primary);
  font-weight: 500;
}

/* 庆祝动画 */
.celebration-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
}

.celebration-modal {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 48rpx;
  background: var(--color-bg-card);
  border-radius: 32rpx;
  box-shadow: var(--shadow-xl);
}

.celebration-icon {
  font-size: 96rpx;
  margin-bottom: 24rpx;
  animation: celebrateBounce 0.5s ease;
}

@keyframes celebrateBounce {
  0% { transform: scale(0); }
  50% { transform: scale(1.2); }
  100% { transform: scale(1); }
}

.celebration-title {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--color-text);
  margin-bottom: 12rpx;
}

.celebration-desc {
  font-size: 28rpx;
  color: var(--color-text-secondary);
  margin-bottom: 32rpx;
}

.btn-celebration {
  width: 300rpx;
  height: 80rpx;
  background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
  color: #fff;
  font-size: 28rpx;
  font-weight: 600;
  border-radius: 24rpx;
  border: none;
}

.btn-celebration::after {
  display: none;
}

/* 挑战详情弹窗 */
.detail-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
}

.detail-modal {
  width: 85%;
  max-height: 80vh;
  background: var(--color-bg-card);
  border-radius: 32rpx;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.detail-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 32rpx;
  background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
}

.detail-icon {
  font-size: 64rpx;
  margin-bottom: 12rpx;
}

.detail-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #fff;
  margin-bottom: 8rpx;
}

.detail-target {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.8);
}

.detail-body {
  padding: 24rpx;
  overflow-y: auto;
  max-height: 50vh;
}

.detail-stats {
  display: flex;
  justify-content: space-around;
  margin-bottom: 24rpx;
}

.detail-stat {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.detail-stat-value {
  font-size: 40rpx;
  font-weight: 700;
  color: var(--color-primary);
}

.detail-stat-label {
  font-size: 22rpx;
  color: var(--color-text-secondary);
  margin-top: 4rpx;
}

.detail-progress {
  margin-bottom: 24rpx;
}

.detail-progress-track {
  height: 16rpx;
  background: var(--color-border);
  border-radius: 8rpx;
  overflow: hidden;
}

.detail-progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--color-primary), var(--color-primary-light));
  border-radius: 8rpx;
}

.detail-timeline {
  margin-top: 24rpx;
}

.timeline-title {
  font-size: 26rpx;
  font-weight: 600;
  color: var(--color-text);
  margin-bottom: 16rpx;
  display: block;
}

.timeline-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.timeline-item {
  display: flex;
  gap: 12rpx;
  opacity: 0.5;
}

.timeline-item.passed {
  opacity: 1;
}

.timeline-item.current {
  opacity: 1;
}

.timeline-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: var(--color-border);
  margin-top: 6rpx;
  flex-shrink: 0;
}

.timeline-item.passed .timeline-dot {
  background: var(--color-primary);
}

.timeline-item.current .timeline-dot {
  background: var(--color-accent);
  box-shadow: 0 0 0 4rpx rgba(232, 184, 109, 0.3);
}

.timeline-content {
  flex: 1;
}

.timeline-day {
  font-size: 24rpx;
  font-weight: 600;
  color: var(--color-text);
  display: block;
}

.timeline-desc {
  font-size: 22rpx;
  color: var(--color-text-secondary);
  display: block;
  margin-top: 4rpx;
}

.detail-footer {
  padding: 24rpx;
  border-top: 1rpx solid var(--color-border);
}

.btn-detail-close {
  width: 100%;
  height: 80rpx;
  background: var(--color-bg);
  color: var(--color-text);
  font-size: 28rpx;
  border-radius: 16rpx;
  border: none;
}

.btn-detail-close::after {
  display: none;
}
